@model Employee
@{
    ViewData["Title"] = "Cập nhật nhân viên";
}

<div class="dashboard">
    <div class="dashboard-header">
        <div>
            <h1 class="dashboard-title">Cập nhật nhân viên</h1>
            <p class="dashboard-subtitle">Chỉnh sửa hồ sơ nhân sự hiện tại</p>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-md-8">
            <div class="chart-card">
                <form asp-action="Edit" method="post">
                    <input type="hidden" asp-for="Id" />
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="FullName" class="form-label fw-bold">Họ và tên</label>
                            <input asp-for="FullName" class="form-control" placeholder="Nhập họ và tên..." required />
                            <span asp-validation-for="FullName" class="text-danger mt-1 d-block"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="Email" class="form-label fw-bold">Email</label>
                            <input asp-for="Email" type="email" class="form-control" placeholder="Nhập email..." required />
                            <span asp-validation-for="Email" class="text-danger mt-1 d-block"></span>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="PhoneNumber" class="form-label fw-bold">Số điện thoại</label>
                            <input asp-for="PhoneNumber" class="form-control" placeholder="Nhập số điện thoại..." />
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="DateOfBirth" class="form-label fw-bold">Ngày sinh</label>
                            <input asp-for="DateOfBirth" type="date" class="form-control" asp-format="{0:yyyy-MM-dd}" />
                        </div>
                    </div>
                    <div class="mb-3">
                        <label asp-for="DepartmentId" class="form-label fw-bold">Phòng ban</label>
                        <select asp-for="DepartmentId" class="form-select" asp-items="ViewBag.DepartmentId">
                            <option value="">-- Chọn phòng ban --</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label asp-for="PositionId" class="form-label fw-bold">Chức vụ</label>
                        <select asp-for="PositionId" class="form-select" asp-items="ViewBag.PositionId">
                            <option value="">-- Chọn chức vụ --</option>
                        </select>
                    </div>
                    
                    <div class="d-flex gap-2 mt-4">
                        <button type="submit" class="btn btn-primary"><i class="bi bi-check2-circle"></i> Cập nhật</button>
                        <a asp-action="Index" class="btn btn-light border"><i class="bi bi-arrow-left"></i> Quay lại</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>